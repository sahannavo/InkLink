package com.project.inklink.enums;

    // You need to define this enum
    public enum  StoryStatus {
        DRAFT,
        PUBLISHED,
        ARCHIVED,
        DELETED
        // add other status values as needed
    }
